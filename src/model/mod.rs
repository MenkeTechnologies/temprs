/// `app` submodule — see file for contents.
pub mod app;
/// `opts` submodule — see file for contents.
pub mod opts;
/// `state` submodule — see file for contents.
pub mod state;

#[cfg(test)]
mod apply_permutation_tests;
